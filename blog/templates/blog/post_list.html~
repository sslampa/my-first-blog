<html>
    <head>
        <title>Stephen's blog</title>
    </head>
    <body>
        <div>
            <h1>A heading</h1>
            <h2>A sub-heading</h2>
            <h3>A sub-sub-heading</h3>
        </div>
        {% for post in posts %}
            {{ post }}
        {% endfor %}
        <div>
            <h2>Hooray!</h2>
            <p>Hi <em>there!</em></p>
            <p><strong>It</strong> <br />works!</p>
        </div>
        <div>
            <h1>Still working on it</h1>
            <p>This will be better in time</p>
        </div>
        {% for post in posts %}
            <div>
                <p>published: {{ post.published_date }}</p>
                <h1><a href="">{{ post.title }}</a></h1>
                <p>{{ post.text|linebreaks }}</p>
            </div>
    </body>
</html>
